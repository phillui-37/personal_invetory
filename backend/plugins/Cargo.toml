[package]
name = "plugins"
version = "0.1.0"
edition = "2021"

[features]
default = []
stub-plugins = []

[dependencies]
domain = { path = "../domain" }
serde = { version = "1", features = ["derive"] }
toml = "0.6"
regex = "1"
